name: React to Source Develop Merge

on:
  repository_dispatch:
    types: [platform-merged-to-develop]

jobs:
  update-kustomize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout caz-conf
        uses: actions/checkout@v3

      - name: Update Dev Overlays
        run: |
          echo "🎯 Platform merged to develop. Time to update conf repo."
