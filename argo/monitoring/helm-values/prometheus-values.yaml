grafana:
  adminPassword: "changeme"  # 🔐 customize this
  service:
    type: ClusterIP

prometheus:
  prometheusSpec:
    serviceMonitorSelector: {}                # 👈 enable discovery of all ServiceMonitors
    serviceMonitorNamespaceSelector: {}       # 👈 across all namespaces
    scrapeInterval: 15s                       # optional: sets global scrape interval